# setup.py
from setuptools import setup, find_packages

version = '0.0.1'

setup(name='PyMeasure',
      version=version,
      description='Python Libraries for measuring things',
      packages=find_packages(),
)
